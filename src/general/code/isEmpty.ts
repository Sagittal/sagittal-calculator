const computeIsEmpty = (array: unknown[]): boolean =>
    array.length === 0

const computeIsSingleton = (array: unknown[]): boolean =>
    array.length === 1

export {
    computeIsEmpty,
    computeIsSingleton,
}
