import { computeHistories } from "./histories"
import { INA_MIDPOINTS, LEVELS_COMMA_MEANS, LEVELS_SIZE_CATEGORY_BOUNDS } from "./snappablePositions"

// TODO: I think you could probably map out this analyzeBounds module and organize it into some helpful subfolders too
//  like you did with N2D3P9's prime exponent extrema module

export {
    computeHistories,
    INA_MIDPOINTS,
    LEVELS_COMMA_MEANS,
    LEVELS_SIZE_CATEGORY_BOUNDS,
}
