import { visualizeBounds } from "./bounds"

// TODO: kinda looks like we're going the route of every module managing its own io/format stuff
//  so perhaps that's what analyzeBounds should call it, and visualize and format could be sibling children of io

export {
    visualizeBounds,
}
