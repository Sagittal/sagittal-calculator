const MAXIMUM_WORTHWHILE_MINIMUM = 0.02
// todo: I would feel better about all these local minima counts of 0 if it printed out the list of all the possible values... because there is a real 0 local minima situation when the area it checks is so small that its entirely within that hypershape where everything is stepped as we discussed
// const MAXIMUM_DEPTH = 10

module.exports = {
    // MAXIMUM_DEPTH,
    MAXIMUM_WORTHWHILE_MINIMUM,
}
